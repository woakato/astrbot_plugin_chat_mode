{
    "isolate_conversation": {
        "description": "会话历史隔离",
        "hint": "开启后，日常聊天与角色扮演分别绑定独立的对话上下文，切换模式时自动切换对话，两种模式的聊天记录互不进入对方的上下文窗口；关闭后共用同一段对话历史。",
        "type": "bool",
        "default": true
    },
    "filter_memory_style": {
        "description": "记忆召回风格清洗",
        "hint": "日常模式下，自动清除长期记忆插件（LivingMemory、MemoryCompanion）注入的记忆内容中的括号动作描写（如（笑）（轻轻拍了拍你的肩）），保留记忆信息本身，防止角色扮演的表达方式串入日常聊天。",
        "type": "bool",
        "default": true
    },
    "inject_normal_guard": {
        "description": "注入日常模式表达提醒",
        "hint": "日常模式下在系统提示词末尾追加一段提醒，强调遵守人设的说话方式，且不要模仿记忆或历史中出现的括号动作描写。",
        "type": "bool",
        "default": true
    },
    "rp_prompt": {
        "description": "角色扮演模式规则",
        "hint": "进入角色扮演模式后追加到系统提示词末尾的规则文本。需要明确声明其优先级高于人设中关于表达方式的限制（如禁止括号动作），否则模型可能因人设禁令而不敢使用括号。",
        "type": "text",
        "default": "# 当前对话模式：实景角色扮演（RP Mode）\n以下规则的优先级高于人设（Persona Instructions）中任何关于表达方式的约束：\n- 人设中「禁止使用括号表达动作」等限制仅针对日常聊天，在当前模式中解除。\n- 使用括号（优先使用中文全角括号）描写动作、神态、环境与心理活动，配合对白推进剧情，保持画面感和沉浸感。\n- 人设中的性格、身份、关系与知识设定保持不变，仅表达形式切换为角色扮演式。\n- 全程保持在角色内，不要以 AI 助手的口吻解释、总结或出戏。"
    },
    "normal_guard_prompt": {
        "description": "日常模式表达提醒",
        "hint": "日常模式下追加到系统提示词末尾的提醒文本。",
        "type": "text",
        "default": "# 当前对话模式：日常聊天（Daily Mode）\n- 严格遵守人设中的说话方式要求，禁止使用括号或星号描写动作、神态。\n- 记忆或历史记录中若出现带括号的动作描写，那只是以往角色扮演会话的存档内容，仅作背景参考，严禁在日常聊天中模仿这种表达方式。"
    },
    "admin_only": {
        "description": "仅管理员可切换",
        "hint": "开启后，只有管理员可以执行 /rp on、/rp off 切换模式，避免群聊中被他人误切换。",
        "type": "bool",
        "default": false
    },
    "rp_image_enabled": {
        "description": "RP 模式场景插图",
        "hint": "角色扮演模式下，每轮对话按概率联动 NAI 生图插件（astrbot_plugin_nai_image）生成场景插图：由当前模型根据场景设定与双方动作描写自动撰写英文提示词后生图发送。未安装 NAI 插件时自动跳过。",
        "type": "bool",
        "default": true
    },
    "rp_image_probability": {
        "description": "插图触发概率",
        "hint": "RP 模式下每轮对话触发生图的概率，取值 0~1，例如 0.2 表示每轮 20% 概率。",
        "type": "float",
        "default": 0.2
    },
    "rp_image_cooldown": {
        "description": "插图冷却时间（秒）",
        "hint": "同一会话两次场景插图的最小间隔秒数，防止连续刷图。",
        "type": "int",
        "default": 300
    },
    "rp_image_prompt_provider_id": {
        "description": "Tag 生成模型",
        "hint": "指定用于提炼生图 Tag 的 LLM 模型 ID。留空则默认使用当前会话正在使用的模型。",
        "type": "string",
        "_special": "select_provider",
        "default": ""
    },
    "rp_image_prompt_timeout": {
        "description": "Tag 提取超时时间（秒）",
        "hint": "调用 LLM 提炼生图 Tag 的最大等待超时时间（秒）。默认 60 秒，若模型响应较慢可适当调大。",
        "type": "int",
        "default": 60
    },
    "rp_image_gen_timeout": {
        "description": "NAI 生图超时时间（秒）",
        "hint": "调用 NAI 生图插件生成图片的最大等待超时时间（秒）。默认 240 秒，遇到网络卡顿或排队时可调大。",
        "type": "int",
        "default": 240
    },
    "rp_image_prompt_extra": {
        "description": "提示词附加要求",
        "hint": "撰写生图提示词时附加的固定要求，例如统一画风：anime style, soft lighting, cinematic composition。",
        "type": "text",
        "default": ""
    }
}
